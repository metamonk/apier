# API Configuration
# Lambda Function URL - find in AWS Amplify Console → Backend Resources → "TriggersApiUrl"
VITE_API_URL=https://your-lambda-url.lambda-url.us-east-2.on.aws

# API Key - retrieve from AWS Secrets Manager
# aws secretsmanager get-secret-value --secret-id zapier-api-credentials-{stackName} --query SecretString --output text | jq -r '.zapier_api_key'
VITE_API_KEY=your_api_key_here

# Dashboard Configuration (optional)
# Auto-refresh interval in milliseconds (default: 10000 = 10 seconds)
# VITE_REFRESH_INTERVAL=10000

# Environment identifier (for display/logging)
# VITE_ENVIRONMENT=production
