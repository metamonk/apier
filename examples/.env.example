# Zapier Triggers API Configuration

# Your Zapier API key (obtain from AWS Secrets Manager)
# This is the password used to authenticate with the API
ZAPIER_API_KEY=your-api-key-here

# API Base URL (optional - defaults to production)
# Use this if you want to test against a different environment
# API_BASE_URL=https://ollzpcmoeaco4cpc773nyz7c5q0zumqi.lambda-url.us-east-2.on.aws
